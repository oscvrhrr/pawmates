export interface ILocation {
    zip_code: string
    latitude: number
    longitude: number
    city: string
    state: string
    county: string
}

export interface ICoordinates {
    lat: number;
    lon: number;
}